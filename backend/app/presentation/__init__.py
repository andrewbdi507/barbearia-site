"""Presentation — API REST Layer.

api/         → FastAPI app factory, rotas, health checks
middleware/  → Request ID, Security Headers, Error Handler, CORS
schemas/     → Pydantic models (request/response)
dependencies/→ FastAPI dependency injection
"""
